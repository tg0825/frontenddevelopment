// namespace.js
if (!naver) {
    /**
     * @namespace
     */
     var naver = {};
}

/**
 * @namespace
 */
naver.model = naver.modal || {};

/**
 * @namespace
 */
naver.collection = naver.collection || {};

/**
 * @namespace
 */
naver.view = naver.view || {};
